// 文章评论前端页面
  1. 正常显示页面
    1. 在admin下新建一个comments文件夹将将文件放入文件家中 引入头部左侧边栏... 
    2. 在页面加载是发送ajax请求, 指定后端借口地址 接收后端返回数据
    3. 将接收到的数据通过模板引擎渲染的页面上
        1. 引入模板引擎库文件;
        2. 设置数据和模板
        3. 调用template函数解析模板
        4. 将接续好的模板填入到tbody中
// 轮播图管理
  1. 正常显示页面
  2. 页面加载发送ajax请求,调用获取图片信息的数据借口获取数据 
  3. 调用模板引擎 将数据渲染到页面上
    1. 引入入文件
    2. 定义模板
    3. 解析模板
    4. 添加到页面上
// 添加新轮播图
  1. 为添加按钮绑定点击事件;
  2. 点击获取表单数据, 发送ajax请求 这里可以使用FormData发送数据
  3.接收到后端返回的数据判断并 渲染到页面上
   接收后端返回的结果利用模板引擎渲染到页面上
    1. 引入入文件
    2. 模板数据
    3. template方法解析模板
    4. 讲解好的字符串追加到页面上
// 左侧管理菜单
   1. 设置各个页面之间的超链接(推荐绝对路径);
   2. 下拉菜单展开和手气状态, 
         in 下拉菜单展开
         active 高亮显示
         collapsed 箭头向右
// 实现思路 
    $_SETVER('PHP_SELF')中保存了url地址
    使用explode 将$_SEVER['PHP_SELF']通过/切割为数组
      假设url地址:  /admin/cate/addcate.php
$arr = [
	0 => ,
        1 => 'admin',
	2 => 'cate',
	3 => 'addcate.php'
];  
    通过$arr[2]来判断文章菜单是收起还是展开状态
    是否高亮,  箭头向左还是向右;
// 前端展示页面,
   1. 页面结构化, 将固定的结构抽取出来 删除页面上的抽取的结构引入结构方便后期维护;
   2. 读取栏目, 读取ali_cate表中的cate_show=显示的栏目  将结果循环到页面上
   3. 随机推荐
        1. 读取数据
        2. 显示数据
    4. 图片滚动 ;
        1 取数据
        2 显示数据
    5. 焦点关注 
        1. 在atricle表中设置一个字段 布尔值 1焦点 2非焦点
        2. 编写sql语句时使用 where 筛选出焦点新闻, 按照发布事件逆序排列,取前5条
        3. 显示数据, 因为数据结构不一样只有第一条数据需要 large   类样式

    6. 最新发布 ;   
     1. 查询数据
        多表联合查询
     2. 显示数据
     3. 

